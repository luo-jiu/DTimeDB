/**
 * 这是sql语句对应的类型
 */
#pragma once
namespace ctl{
    enum TypeId{
        INVALID=0,
        BOOLEAN,
        TINYINT,
        SMALLINT,
        INTEGER,
        VARCHAR,
        TIMESTAMP
    };
}
